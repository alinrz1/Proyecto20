@model IEnumerable<Proyecto20.Models.Almacen>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Proveedores.Nproveedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nproducto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Npresentacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Proveedores.Nproveedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nproducto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Npresentacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Almacen }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Almacen }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Almacen })
        </td>
    </tr>
}

</table>
